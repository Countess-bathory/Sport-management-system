import { TrColorDirective } from './tr-color.directive';

describe('TrColorDirective', () => {
  it('should create an instance', () => {
    const directive = new TrColorDirective();
    expect(directive).toBeTruthy();
  });
});
